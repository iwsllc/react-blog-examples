{
  "name": "@iwsio/react-blog-examples",
  "version": "0.4.0-alpha.2",
  "description": "Example React provider to show one way of using a modal via context API and bootstrap 4 styling.",
  "author": "Nathan Bridgewater",
  "license": "BSD-3-Clause",
  "workspaces": [
    "demo",
    "package",
    "demo-bootstrap"
  ],
  "scripts": {
    "build": "npm run build --if-present -w package",
    "version": "git add .",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.8",
    "daisyui": "^2.42.1",
    "postcss": "^8.4.19",
    "postcss-cli": "^10.0.0",
    "tailwindcss": "^3.2.4"
  }
}
